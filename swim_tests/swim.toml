name = "proj"

verilog.sources = ["src/parametrized.sv"]

preprocessing = ["./extract_stdlib_asserts.py > src/stdlib_asserts.spade"]

[compiler]
path = ".."

[simulation]
testbench_dir = "test"
python_deps = ["pytest"]
