enum X {
    A,
    B{x: bool}
}

entity sub(input: X) -> bool {
    let local = input;
    true
}

pipeline(1) subpipe(clk: clock) -> bool {
        let var: int<8> = 0;
    reg;
        true
}

entity top(clk: clock, input: X) -> bool {
    reg(clk) in_reg: int<8> = 0;
    let _ = inst(1) subpipe(clk);
    inst sub(input)
}
